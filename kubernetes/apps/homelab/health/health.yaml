apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: health-checker
  namespace: homelab
spec:
  project: default
  source:
    repoURL: https://github.com/chammanganti/homelab
    targetRevision: HEAD
    path: kubernetes/resources/homelab-health
  destination:
    server: https://kubernetes.default.svc
    namespace: homelab
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
