apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: health
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: health
subjects:
  - kind: ServiceAccount
    name: health
    namespace: homelab
