apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: demo-apps-tls
  namespace: homelab-demo
spec:
  secretName: demo-apps-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "hld.chammanganti.dev"
