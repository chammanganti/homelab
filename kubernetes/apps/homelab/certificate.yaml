apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hlbe-tls
  namespace: homelab
spec:
  secretName: hlbe-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "hlbe.chammanganti.dev"
